Getting started TDD with Jasmine, Karma and PhantomJS

Dependencies:

- `karma-cli`
- `phantomjs`
- `karma-jasmine`
- `karma-phantomjs-launcher`

Install with:

    $ npm install

To run Karma tests (runs in autowatch mode by default):

    $ npm test

Alternatively run:

    $ karma start karma.conf.js
